# Experimental protocol

Use identical tasks, prompts, decoding parameters, timeouts, evaluation code, and environment for every baseline. Record model/provider versions, run dates, latency, tokens, errors, and task-level scores. Audit generated ground truth before publication.
