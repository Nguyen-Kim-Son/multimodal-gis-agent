# Task schema

Each YAML task has six sections: `metadata`, `datasets`, `input`, `output`, `evaluation`, and `runtime`.

The Pydantic implementation is in `tasks/schema.py`; semantic constraints are in `tasks/rules.py`.
